import SCRIPT from './SCRIPT';
import GROUP from './GROUP';
import POPVALUE from './POPVALUE';

export enum E_TQL_MAP {
    SCRIPT = 'SCRIPT',
    GROUP = 'GROUP',
    POPVALUE = 'POPVALUE',
}

const MAP = {
    SCRIPT: SCRIPT,
    GROUP: GROUP,
    POPVALUE: POPVALUE,
};

export default MAP;
