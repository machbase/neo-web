import type { RawFetchSampling } from '../utils/fetch/FetchTypes';

export const FFT_INTERVAL_UNITS: string[] = ['ms', 'sec', 'min', 'hour'];

export const CREATE_CHART_MAX_SELECTED_COUNT = 12;

export const RAW_FETCH_SAMPLING_DISABLED: RawFetchSampling = { kind: 'disabled' };
