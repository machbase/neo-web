# Normalization

`Normalized` means "converted into one standard, predictable form."

Examples:
- `"42"` or `42` -> `42`
- nested or mixed data -> one flat, consistent shape

Why the name:
- it comes from `normal`, meaning the standard form
